{
  "name": "opencode-see",
  "version": "0.1.0",
  "description": "OpenCode plugin: describe images using free Gemini, Groq, and Cerebras vision models with configurable fallback order.",
  "type": "module",
  "main": "dist/plugin.js",
  "types": "dist/plugin.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/test/*.test.js"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "vision",
    "gemini",
    "groq",
    "cerebras"
  ],
  "license": "MIT",
  "dependencies": {
    "@opencode-ai/plugin": "^1.18.16"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  }
}
